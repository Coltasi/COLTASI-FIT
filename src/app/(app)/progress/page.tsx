import { ComingSoon } from "@/components/coming-soon";

export default function ProgressPage() {
  return (
    <ComingSoon
      title="Progress"
      description="Body composition scans and lifting progress will live here — not built yet."
    />
  );
}
