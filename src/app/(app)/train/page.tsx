import { ComingSoon } from "@/components/coming-soon";

export default function TrainPage() {
  return (
    <ComingSoon
      title="Train"
      description="The Split, kettlebell sessions, and custom workouts are next up — this tab isn't built yet."
    />
  );
}
